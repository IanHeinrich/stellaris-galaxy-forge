//! What the map, the ops and the validator read a document as: the galaxy graph, the
//! names it carries, and the statement readers both formats build it from.

pub mod galaxy;
pub mod name;
pub(crate) mod read;
