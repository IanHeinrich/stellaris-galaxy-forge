/** How long a burst of detail requests is gathered before the queue goes out. */
export const DETAILS_DEBOUNCE_MS = 100;

/** System ids per `get_system_details` call. */
export const DETAILS_BATCH = 200;

/** Localisation keys, or name templates, per resolution call. */
export const NAMES_BATCH = 500;
